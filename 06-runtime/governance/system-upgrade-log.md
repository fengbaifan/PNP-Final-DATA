# 系统升级记录

## 工作流复刻初始化

- 原工作流按字节复刻，来源及哈希见 workflow-copy-manifest.json。
- 新增空数据目录与复用说明，不复制源项目知识和历史验收结果。
- 未执行新领域语义适配，未初始化 Git，未启用或验证客户端 Hook。
- 复制与回归验证结果见交付文件夹的 validation-report.json。
- 新项目正式运行后在本文件追加其自身升级记录。

## 领域迁移：信息图表史 → 《赞助人与画家》（2026-09-09）

### 目标与范围

把知识蒸馏系统从旧领域整体迁移到新领域《赞助人与画家》：巴洛克时期意大利艺术与社会。写入范围：01-domain 全部 6 个文件、项目标识（README、pyproject、02-sources 登记）、ingest/verify/enrich 参考文档、4 个脚本常量、5 组测试与 portable fixture、05-outputs 页面、客户端适配、运行状态重建。禁止改写 02-sources 来源本体；旧领域的知识元、数量、健康分数与验收结论一律不随迁。

### 领域模型决策（用户确认）

- 沿用 A–E 编码框架，五个维度重新定义并全部标为 pilot（赞助与市场机制 / 艺术家与职业网络 / 城市与区域艺术中心 / 宗教权力与机构 / 作品风格与图像志）。
- KU 双语约定：正文中文为主，人名/书名/术语附英文原文，引文保留英文原文。
- 项目命名：Patrons and Painters；domain slug 为 patrons-and-painters。

### 关联规则变更：02-sources 顶层登记文件可更新（用户明确授权）

- 原规则：02-sources 只追加，不改写、不删除。
- 新规则：来源本体（子目录）只追加；顶层登记类文件可更新、不可删除。
- 权威落点同步：AGENTS.md 规则 3、.agents/pipeline.md、distillation-system-contract.md、encoding-check、system-review、README.md。

- 可执行门禁：scripts/agent_guard.py 新增登记文件判定（父目录为 02-sources 的文件；Update 放行、Delete 仍阻断）。
- 行为测试：tests/test_agent_guard.py 新增 3 项，全部通过。

### 修复项

- taxonomy.md Structure Nodes 段层级倒置修正为 domain→dimension→theme→topic。
- 悬空引用（current-health.json、候选账本）在运行状态重建中补齐。
- 02-sources/README.md 旧模板文案更新为实际目录结构。

### 验收证据

- 便携回归全部通过（含新增 guard 测试）。
- 版本一致性（AGENTS/README/pyproject 三处 v5.3.0）通过。
- 未解决项：试点摄入尚未执行；05-outputs 图谱数据待生成后刷新；完整 closure 门禁待首次摄入后运行。

## 本地与远程同步检查（2026-09-09）

- 用户授权同步本地与远程 main。同步范围包含既有领域迁移、第一章试点及其运行工件。
- 修复同步阻断：候选 envelope 的 origin_type 统一为 source、knowledge_unit 拼写改为 unit，保留原候选决定与 approved 状态，并更新工作包输出指纹；为 KU 中含冒号的 YAML 标量加引号，保留原文。
- 补齐知识类型导航、输出登记与二维遗留模板完整性清单，重建派生索引；README 按当前路由与已存在的 10 个 KU、5 个 claim 登记修正。
- 完整 sync closure 通过，254 项测试及 16 项子测试通过；pre-commit 规则漂移为 0，第一章 compact-v4 包校验通过。
- 上节“试点摄入尚未执行”为迁移时快照。当前仍有来源处理链缺口、外部验证、Theme/Topic 挂载及候选写回状态收口待办；本次未重新进行来源语义验收，机械通过不表示这些工作完成。

## v6.0.0：Codex 单入口与六阶段重构（2026-09-09）

### 实施过程

- 依据 user-revisions.md 的 REV-005 开始实施；此前“只审阅”的边界已被用户本次授权取代。Git 提交/推送不在本次授权范围。
- 先核对客户端、目录联接、Skill 与检查脚本依赖；自动审批曾拒绝一次批量清理命令且未执行，随后采用逐项文件删除和经核实的非递归联接清理完成。
- AGENTS.md 为 Codex 唯一总入口，pipeline 只管阶段交接，.agents/skills 是同一系统的唯一技能根。移除 .claude、CLAUDE.md、.codex/hooks.json、.agents/settings.json 和失去调用方的 agent_guard.py 及其旧 Hook 测试；运行环境权限仍由实际 Codex 控制，不声称自动 Hook 生效。
- 18 个 Skill 收敛为 8 个：保留 ingest、verify、enrich、synthesize、compose、inspector、system-upgrade；lint 改为 relate。reconcile 的冲突职责并入 verify；build-hierarchy/evolve-hierarchy/retrospect 并入 synthesize；query/knowledge-graph 并入 compose；read-full/encoding-check/audit-confidence/system-review 并入 inspector。保留必要共享字段和证据契约，删除重复的客户端迁移指南 REUSE.md，复制来源清单仍作为历史证据保留。
- 第一部分按摄入、处理、知识元、对齐、补足、关系交接；第二部分后续明确启动。阶段过程与结果分开存储，同一对象固定文件更新。
- 删除 closure 内自动关系候选生成步骤；现有候选索引只索引账本，不能解释成知识发现。初期完整层级挂载标记暂不开展，缺失计数仍可见。
- 用户原话持续追加，当前要求与过程解释分开；不虚构其他会话记录，不把普通问答变成新规则。

### 结果与验收

- 权威契约、导航和检查实现已调整；定向回归 50 项通过，最终完整 closure 通过（245 项测试）。新增直接 CLI 回归覆盖了包导入方式差异；修复后命令行审计可直接运行。
- Skill 注册校验通过：8 个 leaf，名称/触发词唯一，reference 无孤立或断裂；规则漂移为 0，第一章 compact-v4 校验通过。
- 两份冗余 guards 说明已移除，其证据/状态边界保留在 AGENTS、verify 与风险契约中；没有削弱实际 Codex 环境权限。
- 第一章新增固定过程/结果接续入口，依据既有工件说明历史状态；不补造对齐、补足和关系分析过程。
- 生成投影的末次刷新与重复生成哈希检查在本地完成；未获提交授权，因此不执行基于 HEAD 的 --check-generated，不以自动提交规避这一边界。
- 收尾实测：14 个生成文件重复生成后的字节哈希一致，新增/修改 Markdown 的本地链接无断裂；.claude 和 .codex 目录均已不存在，磁盘上仅有 8 个 Skill。Git diff 确认原始来源、知识元正文、claim 登记和第一章历史 manifest/候选账本未改动。

### 目录匹配评估（REV-006，2026-09-09，只评估）

- 结论：保留 01–06 顶层业务职责，局部调整内部目录与导航；不按六阶段复制六套知识元，不为两部分再套两层根目录。
- Skill 仍保留历史分类数字，enrich 位于 06-growth，而现行职责是第一部分第五阶段。建议八个 Skill 直接置于 .agents/skills/<skill-name>/，顺序与阶段由 pipeline 和 phase 声明表达，消除目录数字被当作执行阶段的歧义。
- 01-domain/naming-conventions.md 仍列出已不存在的 02-multisource、04-relations、09-display，并称其为 Skill 阶段；需要随目录调整修订。此文字残留未被上一轮机械检查识别，不能用规则漂移为 0 推导导航内容已全部审查。
- pipeline 把摄入过程放入 02-sources/<source-id>/process.md，该区域又要求只追加，与过程文档持续版本更迭存在职责张力。建议摄入过程统一放在 03-processing 的过程文件，来源层只保留资产与登记。
- 03-processing 使用包内 process/results，04-knowledge 使用 process/results 下对象文件，当前都能表达职责；可保留稳定 id 和固定文件，并统一入口名称、阶段状态写法。既有 manifest/source-map 等证据不为外观整齐而搬动。
- 05-outputs/README.md 仍介绍 drafts/exports/retrospects，没有介绍已新建的 process/results；实际 drafts 只有 README 和 .gitkeep，exports 只有 .gitkeep，retrospects 不在当前目录。建议过程与草稿合并到 process，定稿说明及导出结果归入 results，保留实际 index 导航及单一当前页面入口，避免再复制一套 site。
- 06-runtime/checkpoints 和 traces 各只有 README 与 .gitkeep，没有真实业务记录；长任务恢复状态已规定归工作包。建议核对引用后清理这两处空的全局记录入口，保留真实历史证据。
- 本轮仅记录评估，未迁移目录、未修改上述命名规范或工作流契约、未运行全套测试、未提交或推送。
- 未改写原始来源、既有知识元或历史语义验收；已有 10 个 KU/5 个 claim 的研究状态不因系统重构自动晋升。

## v6.0.1：从 01 开始的目录修订（REV-007，2026-09-09）

### 实施过程

1. 记录用户原话“应该从01开始吧，现在开始修订。”，以本次授权承接 REV-006 的目录评估；不另建报告。
2. 从 01-domain 开始重写 naming-conventions.md，并更新领域导航：01–06 编号表示内容职责，摄入前读取领域约束，六个阶段不各建一套知识正文。删除旧 Skill 分类、按脚本名推定权限及逐步自动提交等失效规定。
3. 更新 02-sources 的登记说明和 pipeline/ingest：来源资产与登记留在 02；摄入过程和阶段结果归入 03 的同一稳定工作包，按阶段 1、2 分节。既有来源历史记录不搬动。
4. 更新 03、04 的交接说明，知识元正文、断言与历史 compact-v4 工件保持原位。修正领域覆盖规则中“仅冲突才 verify”的歧义：阶段 4 仍做身份/表述对齐，有冲突时才开展完整冲突裁决；后续维度重评过程与结果归 04，不冒充系统治理日志。
5. 收拢 05：草稿修订过程归 process，定稿说明与按需导出附件归 results；保留现有主页面、导航和历史二维快照。旧 drafts、exports 没有业务成果，仅说明与占位文件，经核实后移除，并修订输出登记。
6. 收拢 06：全局 checkpoints、traces 只有 README 和 .gitkeep，均移除；工作包恢复状态继续使用 automation，业务阶段过程与完成依据使用业务目录 process/results。
7. 八个 Skill 扁平迁移，更新活跃文档、关系 schema 加载器、检查脚本、测试、结构导航、运行导航与派生清单的路径。Skill 注册快照移除已无意义的 category，继续由 phase 表达适用部分；新增对旧嵌套路径的拒绝校验及回归测试。
8. AGENTS、pipeline、README、pyproject 的系统版本统一为 v6.0.1；README 更新从 01 领域约束开始的流程图。

### Skill 迁移映射

以下路径均相对于 `.agents/skills/`，整个技能目录连同必要 references 移动，不复制第二份。

| 原目录 | 当前唯一目录 |
|---|---|
| 00-coordination/system-upgrade | system-upgrade |
| 01-intake/ingest | ingest |
| 03-verification/verify | verify |
| 05-quality/relate | relate |
| 06-growth/enrich | enrich |
| 06-growth/synthesize | synthesize |
| 07-output/compose | compose |
| 08-inspection/inspector | inspector |

七个旧分类目录及其中三份冗余导航 README 移除；另移除 05 的 drafts/exports 与 06 的 checkpoints/traces，共清理 11 个经核实的空目录。删除的是无成果内容的说明与占位文件，不包括来源、知识正文、不可替代证据或历史页面。

批量空目录删除命令曾被自动批准审查拒绝，未执行；返回理由仅为 blocked by policy。随后逐项验证绝对路径位于仓库内、不是联接且目录为空，以 .NET 非递归空目录删除完成，未绕过权限扩大删除范围。

### 当前结果与验收

- 目录职责从 01 至 06 已依次修订；执行入口仍为 AGENTS，唯一 Skill 根为 .agents/skills。当前业务位置以 naming-conventions 与 pipeline 为准，不以本日志替代现行契约。
- 最终全套回归：246 项通过。首次全套检查发现输出测试样例仍写入旧 drafts 目录，修正测试夹具后定向 3 项与最终全套均通过。
- 系统升级链 13 项通过，八个 Skill 注册及直接引用检查通过；规则漂移为 0，第一章 compact-v4 包校验通过。
- 刷新后闭环检查通过；14 个派生文件重复生成前后的字节哈希一致。首次刷新时产生的旧导航状态信号已随导航和健康快照一致化消除。一次终端输出遇到 GBK 编码异常，仅影响摘要打印，改为显式 UTF-8 后核验成功。
- Git 差异核对：02-sources 仅顶层 README 改变；原始来源、知识元正文、claim 登记及第一章 manifest/source-map/semantic-units/candidate-ledger/summary 相对 HEAD 未改变。04 中仅更新了层级导航的 Skill 引用及既有本地过程/结果入口，不据此增加研究完成度。
- 当前仍有既有 source-to-processing 数据流缺口、外部验证和候选写回状态待办；本轮未做新语义阅读、知识发现、知识验收或页面开发。
- 本地目录修订已完成；未提交、未推送，不声称远端已获得 v6.0.1。

## REV-009：知识发现与涌现机制的理解纠正（2026-09-09，审读记录）

### 核对发现

- domain-registry.md 把项目名称预先设为 Domain，并要求 5 个 pilot 维度中至少 3 个晋升 core；dimension-registry.md 预先设计 A–E 五个维度，并按数量与不可归类条件决定新增结构。
- workflow-overrides 和 synthesize 优先要求知识进入已有结构；这实际偏向维护预设分类，而非从材料中生成结构。将预设改称 pilot 并未消除这一偏差。
- 磁盘 10 个试点 KU 均已填写 primary_domain 和 A–E 中的 primary_dimension，而 structure 的四类正式节点尚未 materialize。这些字段反映先前分类安排，不能作为经过知识涌现形成结构的证据。
- 先前机械测试只证明路径、格式及既有契约一致，没有验证这套机制是否符合用户研究意图；此前系统完成结论不包含本次确认的概念纠正。

### 修正后的机制理解（Agent 解释，以用户 REV-009 为依据）

可以约定 KU、Topic、Theme、Dimension、Domain 的表达层次，但各层具体内容、名称、数量、边界和成员应由知识分析形成。生成方向为 KU 及其有证据关系 → Topic → Theme → Dimension → Domain。成果形成后才可按反向层级导航展示，不能把展示顺序当作发现顺序。

涌现不是把对象装进更大的集合，而是在下层对象的关系中辨认上层才显现的语义组织：KU 之间呈现具体问题或模式，形成 Topic；Topic 之间的关联与差异形成 Theme；Theme 之间形成可解释的分析维度；Dimension 之间形成较完整的问题领域。每次提升均应解释成员为什么共同成立、形成了什么新的组织或解释意义、适用边界及不确定性，并能追溯下层证据。不能以共现、相同标签或固定数量替代这些判断，也不能以涌现为名新增无来源的事实断言。

发现是识别、提出、比较和检验新联系与解释的研究活动；涌现是其中逐级形成知识组织与高层意义的机制。层级产出是可审查的研究结果，后续知识增长可能修订、拆分或推翻它；无需填满每一层，也不要求每个 KU 有唯一归属。新材料与反例可反馈到下层重新审查，但不能从既有上层标签倒推出材料事实。

第一部分仍按摄入—处理—知识元—对齐—补足—关系执行，为第二部分积累可追溯材料与关系。第二部分启动后才正式开展涌现分析及发现；当前不为凑齐 Topic、Theme、Dimension、Domain 制造内容。允许摄入前确定材料范围、来源版本和问题背景，但项目名称、书名或取材边界不能直接充当已经涌现的 Domain。

### 对现行系统的影响与本轮边界

01-domain 应承载必要的研究范围和约束，不能据目录名提前建立正式 Domain。预设 A–E 与具体领域、优先挂载规则、固定数量晋升、依赖上层编码的字段均须在后续实施中重新审视。已有归属属于待复核历史安排，不作为当前框架依据；本轮未删除字段或改写知识元。

用户原话记录为 REV-009，当前需求索引新增 R-14 并明确“概念纠正有效、实施尚未完成”。本轮完成机制审读与解释，没有启动发现、生成结构节点、修改既有事实或新增版本号；未提交或推送。

## REV-010：空项目工作流完整性审查（2026-09-09）

### 范围与结论

已逐一审读 AGENTS.md、pipeline、全部八个 SKILL.md，并检查与起步、成稿、验证、涌现和呈现直接相关的领域规则、正文/字段/层级模板、语义处理协议、候选工作包与页面 schema。不是全仓脚本或所有 reference 的逐行代码审计，没有运行研究流程或以测试分数代替本轮判断。

结论：两部分目标、第一部分六阶段、八个 Skill 的主体分工可保留，但当前契约尚不能视为符合用户目标的空项目工作流。研究结构预设过多，第二部分和若干阶段交接不足；应保留完备能力和证据约束，去掉研究内容、完成状态与不必要的固定工件预设。本轮为审查，不以旧 246 项测试通过证明这些语义问题已经解决。

### 主要问题与必要修订

| 优先级 | 依据与问题 | 影响与必要修订 |
|---|---|---|
| 高 | AGENTS 写“当前执行第一部分”；README、领域登记和模板混入既有试点/旧系统信息 | 用户明确本项目未执行。总入口需将可执行能力、已授权任务、实际执行状态分开；遗留或试填文件不能直接计入本项目有效成果，处置前保留，不假装磁盘为空 |
| 高 | AGENTS 的向下层级表述、领域 A–E 预设、synthesize 优先适配既有结构 | REV-009 尚未在权威契约落实。具体 Topic/Theme/Dimension/Domain 均从 KU 及关系中逐级形成；可以约定节点类别和证据契约，不能预定具体节点及归属 |
| 高 | pipeline 仅对前六阶段设交接表，后续发现/涌现只有一个存储行；synthesize 没有逐层输出要求 | 需补全 Topic、Theme、Dimension、Domain 各层的输入、语义工作、支持依据、边界、输出及停止/回退条件；不要求一次填满所有层级，也不增设四套重复 Skill |
| 高 | body-template 强制“在层级体系中的位置”；字段契约将 last_verified/review_due 列为初稿必需字段 | 成稿先于对齐/验证和涌现，容易迫使填假状态或空结构。初稿仅要求真实身份/内容/来源/状态，验证日期依实际事件记录，复查日期仅有真实安排才写；层级出现后才记录归属 |
| 中 | enrich 将“缺口得到支持或说明证据不足”都写作完成，pipeline 又允许具备条件对象继续 | 必须区分“本次审查完成”和“缺口已解决”；身份或关键证据受阻时相关依赖不交接，非关键缺口可带限制继续，无需補足不等于未检查 |
| 中 | pipeline 允许对象分流，model-semantic-processing 却笼统要求正式写回前无 needs_review | 需明确验收范围和依赖粒度。当前范围中依赖未读语境的对象不能交接；不相关受阻项不必拖住已具备充分上下文的对象。不能任意缩小范围来规避完整阅读 |
| 中 | 来源漂移只写到处理状态重开；各阶段回退散见不同 Skill | 需补齐受影响依赖追踪：来源/对象身份/事实/关系变化，标记受影响 KU、关系、发现及页面待复核，回到最近必要步骤，保留历史决定；不全库重跑 |
| 中 | 工作对象 ID 的说明偏重同一来源，而 synthesize 必然可能跨来源、跨工作包 | 明确 source_id、KU 稳定身份和任务对象关联的区别；发现记录引用多个已有对象和来源，不能强制沿用单章 ID 或复制整个包 |
| 中 | compose 承担呈现，但 schema 混入旧地图案例、1637 节点样例和 debated/disputed 枚举差异，默认既有主页面 | 新项目页面应允许真实空状态、无正式层级、仅具备部分成果；页面范围由任务确定。schema 用中性例子并与当前数据词表一致，历史页面另行标明，不能当新项目已完成展示 |
| 中 | 第一部分强制 compact-v4 五类工件，又增加 process/results；多处混入旧兼容和工具细节 | 先定义必须保留的语义信息，再选最少存储工件；各信息只设一个权威落点，过程写理由、结果写状态和链接，不重复抄写。现有机器协议如保留须明确边界，不能未经依赖检查删除 |

### 八个 Skill 的基本完备合同（修订目标，不是本轮已实施结果）

| Skill | 输入与工作 | 必须可审查的产出与交接 |
|---|---|---|
| ingest | 可定位来源及指定范围；分摄入、语义处理、知识元成稿三步，识别对象与原文断言，核对已有对象 | 来源登记/可读范围；语义理解、覆盖与歧义；有来源的 KU 草稿与必要断言、暂缓清单。三步分别留状态，成稿交 verify；空库检索无匹配是正常起点 |
| verify | KU 草稿、来源与待核问题；身份消歧、名称/版本对齐及必要事实核验，保留冲突两侧依据 | 已对齐/待消歧/有争议对象、裁决和真实验证范围；可用对象交 enrich，关键身份不明对象单列；不能自动推出整篇已验证 |
| enrich | 对齐结果和明确内容缺口；优先已有材料，必要时有针对性补证、更新正文 | 实际补充、支持来源、未解决缺口及其是否阻断交接；新来源或新对象返回相应摄入入口，不自动扩张任务；符合条件交 relate |
| relate | 可用 KU、来源与对齐/补足结果；判断关系端点、类型、方向、语境和证据 | 正式/待证/否决关系与理由、当前图谱；发现身份或出处问题回 verify/ingest/enrich；没有正式关系是合法结果，不自动启动发现 |
| synthesize | 用户启动的发现范围与已有 KU/关系/断言证据；逐级辨认共同问题、问题群、分析视角和领域体系 | 各层候选/成立/暂缓结论，成员及来源依据、新增语义、反例与边界；形成多少层记录多少层，知识支持不足时停止或定向回退；经审查结果交 compose |
| compose | 用户呈现任务、可使用的知识/关系/发现及状态限制；形成内容、设计、页面或其他指定成果 | 定稿、页面/成果入口、使用依据、限制及内容与功能检查结果；无数据如实展示空状态；发现错误退回责任阶段，页面不成为独立证据 |
| inspector | 指定对象及其过程/结果、适用契约；审查语义、证据、覆盖或系统一致性 | 问题、证据、影响范围、是否阻断、应回到哪个阶段及复核结果；自产自查不能称独立验收，不额外重复生成所有业务记录 |
| system-upgrade | 用户修订及现行规则、直接依赖；处理规则和工具一致性 | 最小必要修改、变更理由/映射、验证结果及未完成事项；不改变研究结论、不自动触发生产、也不为小修订持续添加文档版本 |

### 最小交接与空项目验收标准

每次交接至少能回答：本次对象与范围是什么；依据哪个输入版本；实际做了什么；当前成果在哪里；哪些已成立、哪些仍不确定或受阻；下一环节可处理哪些对象及为何。用同一对象现有过程/结果文档表达，不为这些问题再建六份表。

可以事先定义功能、对象基本表达方式、来源证据要求、稳定标识、状态、输出位置和交接边界。不能事先定义具体知识元、Topic/Theme、维度/领域名称与数量、归属、关系、发现结论、验证日期、完成率或成果规模。常用类型与关系词表属于表达规则，不等于已有研究内容；应允许有依据的扩展，避免为填词表制造实例。

空项目应能停留在“尚未执行”，没有来源/知识/结构/页面时既不伪造数据也不报应立即填满的缺口。首次收到明确执行任务后才形成对应对象工件；一般规则仍可提前完整。默认不为固定规模、篇幅、满层级、全库外部核验或满健康分数新增步骤。

推荐修订顺序：先统一 AGENTS 的空项目定位和涌现方向，再补全 pipeline 的两部分与交接/回退，随后修订八个 Skill 及直接模板/字段，最后对齐实际使用的辅助代码和空状态检查。保留八个 Skill，不再拆增同用途技能。

### 本轮完成边界

用户原话与解释已保存为 REV-010；需求索引新增 R-15/R-16，并如实标记待实施。完成本轮系统语义审查与可实施的修订清单；没有改写 AGENTS/pipeline/Skills、删除遗留材料、执行研究任务、增加版本号或运行全套测试。未提交或推送。

## REV-011：门禁、审核和冗余审查补充（2026-09-09）

### 结论与证据

存在过度设计，主要在把批量自动化、历史兼容和多工件追溯要求放进日常语义工作的默认路径；不在六个业务阶段或八个 Skill 的数量本身。还存在相反的问题：第二部分的语义功能与交接不够具体。应同时做“执行负担减法”和“语义职责补全”，不能把文档越短等同于系统越好。

现行 AGENTS/pipeline 已明确阶段交接不自动增加用户审批，runtime-state-machine 也已规定短任务不必创建状态文件。因此不能宣称当前每一步都要求用户批准，也不应为精简删除已明确按需的恢复和证据保护。

具体冗余或不适配：

- 日常写回在 work-package、verify/relate Skill 和 relation-governance 中反复铺设 candidate → approved → exact plan → dry-run → apply → validation → result；重复的是合同描述及要求分别固化多个工件，不是来源证据和判断责任本身。单条语义修订可以在既有过程记录中说明依据、直接更新授权范围内的固定成果并定向核对；批量机器写回、身份合并、覆盖多对象等才需要独立计划/预检/恢复资料。
- compact-v4 的 summary、业务 results/stages、工作包 summary、apply result 都可能叙述同一结果。应指定业务 results 为人工交接入口；必要机器结果只存执行事实并被链接引用，兼容摘要不再复制一套人工报告。
- candidate、decision、review_queue、defer_queue、阶段状态及 file_back_status 分属不同用途，当前容易被误用为每个对象必走的多轮审批。应明确同一判断只作一次；对象阶段状态说明业务进度，候选状态只用于真实候选流转，机器队列仅是执行视图，输出回流仅在有问题/增量时启用。
- 六阶段不是六轮重复审核。成稿判断对象边界与来源支持；对齐检查身份和冲突；补足解决明确缺口；关系检查具体断言，inspector 只做有目标的检查，不逐阶段再走一遍所有步骤。新证据、冲突或范围变化才重开对应问题。
- upgrade-acceptance-gates 的 generated_clean 仍要求生成投影相对 HEAD 无漂移，与 AGENTS/pipeline 对未提交修改采用哈希幂等的边界不一致；分类 README 要求也已过时。应以实际受影响内容的正确性检查为准，提交基线核验留在已授权交付时。
- 高文档版本、旧模型/项目样例和大量历史兼容解释不应占据空项目主流程。当前规则讲清现在如何执行，历史沿革只留既有日志；机器协议兼容需要单独保留，不未经检查重编号。
- 预设维度、固定晋升数量和层级完整度要求是研究偏置，不能保留为所谓质量门槛。涌现取决于语义与证据，不取决于凑满分类或健康分数。

### 保留与条件启用

日常基本路径：读输入及上一阶段结果 → 完成本阶段语义工作 → 保存过程/当前结果 → 核对直接受影响项 → 交接。检查可以并入工作本身，不另设审批阶段；阻断只针对依赖不成立的具体对象。

必须保留：来源可定位、原始材料不被误改；同一对象的身份/版本准确；事实与关系有对应证据；不伪造已完成或已验证；正式成果唯一；变更可追溯。保留这些基本条件不意味着固定多人复核或用户逐步批准。

按条件启用：外部核验只针对必要问题；完整冲突裁决只针对真实冲突；独立审查只在任务和证据要求下采用；机器批量写回使用预检及可恢复提交；长任务启用状态文件；脚本/schema 修改运行相应回归；索引与页面在其输入实际变化、交接需要时刷新。已确认材料不因重复运行而反复审查。

不作为默认动作：每阶段全套测试、全库外部采集、所有索引刷新、固定审核轮数、强制完整层级、为空状态填数据、为通过 HEAD 检查自动提交、为每次小修改生成 plan/summary/report 副本。

### 页面保留与数据更新

用户允许保留现有视觉样式、布局、颜色、字体及适用交互，不要求重新设计页面。具体内容必须读本项目的实际可用数据：节点与类型、正式关系、来源与证据状态、实际涌现的层级、数量统计、筛选选项和说明文案。项目尚未执行时呈现空状态，不用旧项目数据或示例数字装成新结果。

后续新数据产生后，保留样式并调整数据模型到页面的映射；无某层结构则不显示虚构分组，现有分组/筛选若依赖预设 A–E 则改为实际数据驱动。图中有关系不等于已形成 Topic，页面不能反过来要求知识填满预定布局。更新后检查空状态、真实统计、筛选、节点详情、关系与证据链接、已有视觉交互是否仍有效。

### 本轮完成边界

本节补充 REV-010 的整体审查。原话已存为 REV-011，需求索引增加 R-17/R-18；给出可保留、应合并与按条件启用的规则取舍。未执行生产、规则删除或页面修改，未生成新研究数据，未提交或推送。


## 初始化精简调整（REV-012，2026-09-09）

### 执行依据与先行同步

用户授权“先提交并同步仓库。然后开始精简调整”，承接 REV-008 至 REV-011 的审读结论。先完成已有修改及审读记录的提交和推送，再实施本节调整。

先行提交：`d07965d8d5371527fb464455996c164cf704339e`。main 与 origin/main 均为该提交，`ls-remote` 验证远端相同，领先/落后为 0/0，当时工作树干净。首次 fetch 遇到 TLS 连接中断，改用 Git 的 schannel TLS 后端成功完成；没有关闭证书校验。随后精简修改保留本地，未再次提交或推送。

### 调整过程与取舍

1. 先重写 AGENTS、pipeline 与八个 Skill，补齐六阶段的输入、语义工作、产出、交接和必要回退。日常路径为阅读与判断、保存过程/结果、核对直接影响、继续交接；取消固定轮数、重复审核和每阶段全套机器工件的默认要求。
2. 取消具体 Domain、A–E 维度、固定主题骨架、优先挂载和数量晋升门槛。第二部分明确由 KU 及有依据的关系逐级形成 Topic、Theme、Dimension、Domain；允许只形成部分层级、交叉归属、反例、拆分与撤回。没有开展研究或知识发现。
3. 缩减 KU 模板与字段。初稿保留名称、类型、来源及实际创建/更新日期；外部核验、层级和复查日期等按实际事件启用，不能为完整度填假值。claim 与其支持证据分开。
4. compact-v4、evidence dry-run/apply、原子批量写回及恢复协议保留为按需工具契约，不强制普通语义任务使用。移除规则检查中的固定行数、固定措辞及所有文档强制同版本等脆弱要求；系统检查保留入口、引用、数据引用清单与项目版本等实际条件。
5. 普通规则文档取消独立高版本；项目初始化版本为 0.1.0。历史日志和机器协议版本保留，避免把历史记录改成新事实。
6. 保留来源、10 个遗留 KU、5 个遗留 claim 及旧处理证据。新增最小 `04-knowledge/accepted.yml` 只列有效对象引用，当前为空；这是阶段结果登记，不是新的用户审批。第一章结果说明改为遗留清单，不能表示当前研究完成。
7. 主页面保留现有 CSS 和图谱交互，改为真实数据驱动的数字、类型筛选、结构列表、节点详情及依据链接。取消旧人物焦点和固定维度文案；有效数据为空时显示尚未执行。导航、图谱与有效知识统计共用清单；仅有 Topic 时也能呈现，不制造上层结构。
8. 过程与当前结果仍使用固定文件，未新增同用途报告副本。机器投影保留历史库存诊断，并明确它们不表示研究已执行或自动触发开工；没有为追求满分删除历史证据或伪造完成。

### 当前结果

- AGENTS、pipeline、八个 Skill 及其 Markdown 参考文件和 01-domain 规则合计从 3,075 行减少至 1,776 行，约减少 42%；职责和必要证据条件仍保留。
- 有效 KU、claim、正式关系和涌现结构均为 0；页面为初始化空状态。来源资产预存与遗留文件存在不构成已完成研究。
- 过程/结果分别存于业务目录；用户原话、目的解释和本次实施结果沿用既有治理文件记录。
- 权威入口仍只有 AGENTS.md，唯一 Skill 根为 .agents/skills，不另建客户端系统。

### 验证及实际限制

- Python 回归 247 项通过；最后一次治理提示文案调整后，相关 6 项测试再次通过。
- Skill 注册及直接引用有效，八个 Skill 可定位；规则漂移检查 0，精简后的 4 项系统契约检查通过。
- 新增行为验证覆盖：空清单排除遗留对象；登记一个/两个对象时图谱与关系统计同步变化；错误清单不回退到全库；独立 Topic 可保存成员引用且不要求父层。
- 主页面 CSS 与先行同步提交逐块一致。浏览器实际检查过空状态、两节点一关系临时样例、类型筛选、仅有 Topic 的展示、缩放和键盘打开详情，当时未见页面错误；样例未进入知识库。后来补充的来源/成员链接已做静态检查，内联 JavaScript 语法通过；浏览器连接随后中断，未对最后这部分链接重复做浏览器点击验证。
- 历史处理库存仍有 4 条 dataflow 诊断和 15 条候选记录，未作为本项目研究债务或成果，也未在此次系统调整中重做来源语义处理。辅助健康评分不作为当前研究完成判据。
- 本节之后仅核验相关投影、差异及同步基线；本地精简不等于再次远端发布。

收尾核验：受影响投影重复生成的哈希全部稳定，git diff --check 通过；来源、遗留 KU、claim 登记和原处理 manifest 相对 d07965d 未改。临时页面样例及语法检查文件已清理，本地测试服务已停止。再次核对远端 main 仍为 d07965d，与本地 HEAD 相同；精简修改仍在工作树。

## REV-016：语境类型边界与 archive 迁移

用户原话和目的见 user-revisions.md 的 REV-016。语义复核和补漏过程在 03/04 对应业务目录，本节仅记录系统调整。

### 过程与映射

1. 修订 01-domain/taxonomy-registry.md：地名/政治实体、建筑空间/构件作品、保管机构/建筑、艺术图册/文献图录按指称对象区别；archive 包含所有文献，八类不足的实际对象保留类型待决。ingest 补上不能以脚注、无全名或无正式题名批量排除对象的规则。
2. 当前 KU 类型 publication → archive；目录 `04-knowledge/units/publications/` → `04-knowledge/units/archives/`，20 个原文件沿用 slug 迁移（18 个有效、2 个第六章未接收草稿）。仅迁移现有文献路径和类型，不重新创建文献原件。正式关系中的文献路径和类型由权威 KU 重新投影。
3. 原位同步类型枚举、目录扫描、校验/核验路由、生成器、角色 source_publication → source_archive、相应参考文档和测试。外部 publication_year 等字段和历史 relates_to_publication 兼容枚举保留原义，不误改为本项目类型。Wikidata 的 archive 匹配只将已知出版类作为其子集；未识别未出版文献类型不再判为出版物类型冲突，不要求所有档案具有出版信息。
4. 迁移先列精确改动和目录内容，核对全部输入 SHA-256；文件写回失败可恢复，目录移动前验证绝对路径处于当前仓库、无目标覆盖、非联接。原始来源、历史处理账本、用户原话和冻结网页不批量替换。当前结果与清单改活跃引用，历史过程保留旧计数和路径作为变更依据。
5. 仅更新 04 的关系/翻译投影与 Skill 注册导航。浏览页面继续冻结，不为 archive 新导航制造空页面；待第二部分恢复时由已适配的生成器产生。未提交或推送。

### 结果与检查

- 唯一当前类型为 person/institution/place/work/archive/term/procedure/event；无第二个活跃 publication 目录。八个 Skill 保留原分工，不新增审批或研究层级。
- 第一章新增 26 个 KU、接收经索引具名的 1 个既有候选，现 297 KU、12 条断言、173 条显式关系；详细计数与未解决项仅以 04 当前结果为准，不能据这些数目宣称穷尽。
- 234 项 Python 回归通过，含未出版 archive 不被当成出版物冲突的新回归；测试中的预期失败模拟输出不代表真实仓库检查失败。
- 关系一致性检查断链、无效类型、缺反向、弱证据、未备案旧泛型关系均为 0。规则漂移检查最终为 0，Skill 注册已导出。
- 检查期间发现未生成的 archive 页面被误列为现有入口，已改为准确的暂停说明；旧 claim 注册使用 flow YAML，现改为同数据的 block YAML 并保留 total: 12，使已有检查器可正确计数。未为通过检查生成页面或改变断言内容。
- 已保留本次开始时的来源/页面指纹供收尾核验。语义完整性不由机械检查替代；其未决项继续在业务过程与结果中明示。

## REV-017：外部补足、双重身份核对与正文职责

原话及目的见 user-revisions.md REV-017。核查 verify/enrich、正文示例及两个 Wiki collector：当前脚本分别收候选，无 sitelinks/pageprops 双向配对；第一章只做来源内回读与书目/索引核对。查询 Wikidata、MediaWiki、Getty 官方能力说明，未开展具体实体外部核验。

原位修订 AGENTS、pipeline、verify/enrich 及直接参考、ingest 正文说明。配对规则集中在 verify：双向相同 QID 加语义身份一致；缺一侧或粒度不符保留未完成；双站关联不冒称独立事实验证。按类型参考改用 archive，去除未出版档案必需 DOI/ISBN、作品按出版物验证及模型知识充当外证等旧写法。没有新增 Skill、脚本、审批轮次或报告副本。

知识过程与结果澄清外部补足未开始；现有 297 KU、12 条断言、173 条关系及其来源支持状态未变。自动采集/写回接口本次未改，不声称机器已强制执行配对规则。来源本体、页面、知识元和关系未改，未提交推送。

必要检查：规则漂移检查为 0，git diff --check 通过，Skill 注册导航已导出。本次只改规则、正文说明与状态记录，没有改采集代码，因此未重复运行全套回归；这些检查不代表实体语义验证。

## REV-018：知识元标题与描述双语

原话及目的见 user-revisions.md REV-018。检查现有字段：title 可承载双语展示名，name_en 已被翻译索引识别，描述存于正文。沿用这些结构，未新增 title_zh/title_en/description 副本字段或语言文件；AGENTS、ingest 及 enrich 同步职责，普通正文修订必须保持两种语言事实和限定等值。

那不勒斯原有双语标题，现补 name_en，并回读原文位置撰写中文/英文描述；其他 KU 未批量改写。本次不改机器解析代码、来源或网页，不提交推送。规则检查及差异空白检查在下方记录，均不代表全库双语语义验收。

检查结果：规则漂移 0；git diff --check 通过；Skill 导航已导出。那不勒斯中英文描述直接核对了日期、人物、地点身份与计价语境的一致性；未进行全库双语审查。

## REV-019、020：统一元数据、类型内容、关系与证据

用户原话及第三部分确认见 user-revisions.md。采用统一 frontmatter 元数据，内容以双语描述、简单属性表和按类型的多项/时间记录组织，第三部分解释关系、具体证据范围与待补项。详细内容范围集中到已有 body-template.md，不新增一套 Skill、类型存储文件或数据服务。人物规范名全名优先，但未核身份不能填造；历史国籍、权属、名称和存放地需保留时期、角色与依据。

AGENTS、pipeline、ingest、enrich 和关系参考同步。现有 relations 保留 frontmatter 的机器兼容位置，逻辑属于第三部分；正文引用说明，不复制第二套真值。全库迁移没有执行，也没有改工具解析协议。

业务示例在原有 guercino.md：复读来源，补精确行号、双语描述、活动与对象清单、作者评价及书目状态；缺少全名、生平、亲缘等外证如实待补。没有新增正式边、KU 或外部验证状态。源材料、页面保持不动，未提交推送。

检查结果：规则漂移检查为 0，git diff --check 通过，样例新增正文链接的目标文件存在；Skill 注册已导出。直接核对双语描述与原文事实、时间和限定，未声称独立验收或全库迁移。未改解析代码，未重复全套回归。

## REV-021：知识过程迁入 03-processing

原话及目的见 user-revisions.md REV-021。当前知识成果与结果留在 04-knowledge，过程统一回任务对应的 03-processing。

### 迁移与直接依赖

- 04-knowledge/process/patrons-and-painters-chp-1.md → 03-processing/patrons-and-painters-chp-1/process/knowledge.md。
- 04-knowledge/process/patrons-and-painters-chp-6.md → 03-processing/patrons-and-painters-chp-6/process/knowledge.md（仅移动既有历史记录，不启动该章）。
- 旧过程 README 的规则合并进 03-processing/README.md，删除已合并说明和空的旧目录；无内容副本或兼容空壳。

先核实绝对路径均在仓库内、路径无联接、目标不存在，再逐文件移动，SHA-256 均相同。第一章迁移后仅调整两个相对 Markdown 链接；原判断、用户修订和历史状态保留。同步 AGENTS、pipeline、ingest/verify/enrich/relate/synthesize、目录规范、根 README、结果说明和当前需求索引。系统历史日志及用户原话不批量替换旧路径，迁移映射由本节说明。

一次带计算路径的空目录清理命令被工具策略拦截，未执行；随后核对目录为空，使用限定绝对路径且非递归的空目录删除成功。迁移不改变 297 KU、12 条断言、173 条正式关系或外部补足状态；未修改原始来源、知识元及网页，未提交推送。

检查结果：规则漂移 0；git diff --check 通过；两个迁入过程文件及第一章结果中的本地 Markdown 链接检查无断链；活跃规则/脚本/测试无旧过程路径引用。旧目录已不存在，两章任务包均有 stages.md 与 knowledge.md；Skill 注册已导出。第六章未创建结果文档，其历史记录保留不等于重新执行。

## REV-022：入口、Skills 与渐进式分布记录升级

用户原话及目的见 user-revisions.md REV-022。此项是系统检查与升级，详细过程只记本日志，不向 03/04 再追加相同业务报告；研究状态由原有 results 维护。

### 检查范围与发现

检查 AGENTS、根 README、pipeline、01 的约定入口、03/04/06 的目录说明、结构导航及候选入口、全部 8 个 Skill 与 25 份直接参考、注册生成器、相关验证写回接口、测试入口和 CI 配置。02 与 05 的入口只读核查，冻结内容不按新规则批量改写；历史导入清单和用户原话不作当前路径错误自动删除。

1. 实际规范总入口为 AGENTS.md，未发现第二个项目 Skill 根、重复客户端配置或同名 Skill；8 个均为 leaf，4 个 current、2 个 later、2 个 support。原有 25 份参考文件路径均有效，但某些跨 Skill 引用只写简称，按需使用情境不够明确。
2. 04 的 README 漏改，仍指向旧 process；01 与 06 的入口仍称研究未执行。均改为当前结果导航与正确记录位置，不维护第二份进度数字。
3. 结构目录仍继承一个测试领域、五个维度、33 个主题和 Topic 必需 parent_theme 的说法，另有退役 evolve-hierarchy 入口与 pilot 维度候选说明。已纠正为实际知识逐级涌现、父层可尚未形成，当前仍暂停，未创建研究节点。
4. index-layering-contract 把 relation-index/translation-index 当作独立权威索引，与当前 KU relations 为事实输入的规则冲突。已区分有效范围、知识与证据、结果、过程、可重建投影、导航、候选/运行状态和呈现；可先借导航定位，但事实回查对象及来源。
5. scripts/README 仍有固定确认表、统一全量生成/提交收尾和旧 Work/Publication 口径。改为按需工具说明，CI 与本地语义工作区分，不以工具文档增加权限要求。旧 portable/run_tests.py 依赖已退役导入路径并生成预设结构测试材料，且无活跃调用者，已删除；历史导入 manifest 原样保留，portable/verify_copy.py 仅用于原始导出包。
6. verify 无数据参考仍有逐站级联、source_backed_only 混入状态、每次网页研究必须 JSONL 等冲突，已统一为 verify 的双重身份规则、按对象选择外证以及普通编辑/机器接口分界。API 候选输出仍未自动执行双向 QID 核对，不能冒称完成。
7. 验证写回器仍另加顶层验证段，并给没有 version 的普通 KU 自动新增版本号。现已将段落写在第三部分“关系与证据”下，保留相邻关系、未决项和其他段落；旧顶层段在实际写回时归并，重复不明段落阻断；仅已有 version 字段保留兼容递增。没有对真实 KU 执行该接口。

### 渐进式读取与记录的当前实现

- AGENTS 提供目标/边界及实际 Skill 链接；pipeline 提供交接和存储映射；选定 Skill 直接说明工作，再按问题读取明确列出的 reference，不隐式遍历全套。
- ingest、verify、system-upgrade 的参考清单补使用情境；enrich 直接列共享字段和内容规则，synthesize 直接列结构字段，relate 的跨 Skill 词表路径规范化。注册表是派生导航，不是另一套规则。
- 03 的 task-id 包保留 stages.md 与 knowledge.md，实际研究按阶段增量记录；04 results 原位维护当前状态并链接唯一对象；过程中的历史判断保留。完整语义阅读要求不因渐进式读取而缩减为摘要阅读。
- 原话只记 user-revisions；current-requirements 压缩为有效要求、REV 与权威入口索引；系统细节只记本日志。没有为每轮更新在所有目录重复追加报告、创建新规则文件或提高项目版本。
- 05 保持冻结，未来呈现消费新三部分与双语数据的规则写入 compose 参考，页面实际适配不冒称完成。

### 实现与必要验证

复用 audit_rule_drift 扩展目录 README、结构导航/候选入口及实际 Markdown 链接检查，覆盖曾被漏查的位置；AGENTS 中链接形式的 Skill 也核对实际存在，旧 04 process 与旧 portable 测试入口纳入退役检查。新增回归覆盖目录断链、历史链接排除、模板/外部链接不被要求物化、链接式 Skill 缺失，以及验证段嵌套、旧段迁移、不新增版本号、重复段落阻断。

- 62 项相关回归通过，覆盖规则检查、Skill 注册、客户端布局、证据写回/批次路由及收尾接口；扫描范围追加候选入口后又复核了其中 26 项规则测试。未运行全量生成或页面刷新。
- 规则漂移检查为 0，8 个 Skill、25 份唯一直接参考有效，无外部 Skill 根或冗余客户端；注册快照已导出。
- 当前入口及其本地 Markdown 链接检查无断链，git diff --check 通过；详细范围包含当前需求索引。
- 原始来源及冻结输出的 192 个文件与 REV-016 保存的 SHA-256 全部一致，无新增文件。本轮未写知识元、关系或验证状态，未采集具体实体外证。
- 升级是规则、入口与相关辅助行为修正，不代表知识元全库完成双语/三部分迁移、外部补足或独立语义验收。项目版本仍为 0.1.0，尚未提交推送，也未声称远端 CI 通过。

## REV-026、027：对齐与补足分工澄清

2026-09-09。按用户原话修订 pipeline、verify 与 enrich 的直接职责，并更新 current-requirements 的有效要求索引。第四阶段为初步身份对齐，不要求先完成生平、作品等详细字段；第五阶段在双向配对后按需提取 WD 属性、限定词和引用，完整阅读 WP 对象页后提取叙述。局部读取、访问失败、来源冲突和未读原文明确留存。外部关联对象不自动新增 KU 或正式关系。

不新增审批环节、规则文件或版本号。实际五卡补足过程只记 03 的 knowledge.md，当前成果只更新 04 的第一章结果和原有 KU；本日志不复制研究事实。Skill 注册与翻译索引按受影响内容重建，必要检查结果记入该业务过程末尾。

## REV-028–030：角色、双语名称与可读来源定位

2026-09-09。在 ingest 的 body-template 增加有据多角色属性表达，不预先建立细分词表；在共享字段契约及最小示例中明确完整中英文信件题名，以及 sources.location 的章次／印刷页和 sources.sentence_summary 的句意摘要。多处引文分述支持范围，网页不虚造章页。verify 规定英文优先，缺匹配英文对象时按背景查询相关语言，并保留配对要求；访问失败不视作不存在。

有效要求索引仅增加／修订相关条目。本轮曾误将其相对路径多加一层，直接链接检查发现后恢复正确的 ../../ 路径。未新增入口、门禁、重复文档或版本号。实际研究证据保存在 03，当前八卡与状态保存在 04；本日志不复写研究事实。必要检查在业务过程末尾记录。

## REV-032：逐阶段定稿与过程清理

2026-09-09。pipeline 明确当前任务依摄入与处理、知识元登记、对齐、补足、关系顺序收口，每步先定稿再清理过程，不增加审批或新文件层级。ingest 和 03 README 同步明确：完整逐行分析是阶段成果，保存在既有 results/stages.md；process/stages.md 只保留必要裁决和清理依据。

按本次明确清理要求，允许核对 Git 可恢复性及依赖后退役已被当前成果替代的旧 compact-v4 包，不把默认保留旧包理解为永远不能清理。第一章实际整理及恢复定位只记 03 的过程；来源本体和已有下游知识内容保留。候选派生索引刷新属于清除旧待执行入口，不是启动发现；机器快照按实际文件更新，不据分数认定语义完成。

## REV-036：增加 family 家族类（2026-09-10）

按 REV-035 的建议和 REV-036 的执行授权，将有据的家族与支系作为独立 KU 类型。taxonomy-registry 是分类权威；共享元数据只扩充 type 允许值，正文补充原语名称、支系／成员、亲缘联姻、称号继承、赞助收藏与财产沿革的内容范围，不建立空族谱或重复 YAML 属性。verify 区分家族、姓氏页、个人和家户，API 仅提供待语义判断的身份候选；现有 member_of（人物→家族）及 part_of（支系→总族）足够表达当前基本需要，未新增受控谓词、审批或独立 Skill。

分类迁移只涉及已明确身份的三个对象：原目录 `04-knowledge/units/institutions/` → `04-knowledge/units/families/`，文件名分别为 `aldobrandini-family.md`、`borghese-family.md`、`peretti-family.md`，slug、sources、日期和证据状态保留。accepted 与当前结果链接同步迁移，无旧卡副本。REV-034 外证 JSONL 保留采集时的 `institutions/<slug>` 历史键及指纹，每张迁移卡已明写检索该历史键，避免改写取证记录。巴贝里尼混合家族／多个家户卡暂留 institution，并注明这是待分清的混合记录；不能把该暂存分类解释为家族属于机构，也不能把服务者当家族成员。有关范围核对交后续补足，不新增无证对象或关系。

直接辅助依赖已加入 families 扫描与 family 类型：有效集合及内容审计、翻译和关系索引、补足与验证队列、孤立对象／候选扫描、规则漂移检查和 Wikipedia／Wikidata 路由。特别修正 families 不能删末尾 s 得到 family 的情况；Wikidata 的 family 候选不凭任意 P31 或同名获得强配对，也不自动接收整卡事实。LCNAF 专用采集器仍限原有 person/institution 能力，家族不能冒充 CorporateName；需要家族规范记录时按实际来源读取。呈现数据生成器仅补类型名称与颜色映射，未生成或刷新页面；后续发现未启动。

验证：47 项定向回归通过，包括 family 名称误配防护、补足／翻译／有效扫描纳入，以及关系目标的不规则复数处理；脚本语法、当前类型路由与呈现类型契约检查通过。有效 298 KU 中 family 3、institution 17，其余分类数未变；842 条来源定位、173 条关系、160 项既有配对保留。298 行当前目录包含三项家族，312 个 KU 本地链接有效，三张旧路径已退出当前集合。名称索引重建后仍为 260 条（既有五类范围加 family，迁移不增卡），关系索引仍为 173 条 explicit、无推断新增；Skill 派生注册按改动导出。旧健康快照保留采集时状态，不作为本次分类数量依据；当前统计以 04 结果及 accepted 为准。

这是类型能力调整、三卡语义重分类及直接检查，不代表新外部核验或详细补足完成；未执行第六章、涌现、页面、Git 提交或推送。项目仍为 0.1.0，不提高规则文档版本。

## REV-041–042：统一实体分组字段与条目格式（2026-09-10）

REV-041 当时明确只要框架，未写入文件；REV-042 授权按相同逻辑修订其他类型后，补记此前原话并落实模板。使用 system-upgrade，只修订最近的权威规则和直接入口。

在既有 ingest/references/body-template.md 原位整理九类正文规则，取代原来宽泛的类型范围总表及重复说明。正文统一为独立字段、多值条目、分期履历／沿革及本章语境；人物分开姓名、头衔、亲缘与身份，其他八类按自身对象性质列专用字段。建筑保留为 place 的专用小节。共同条目格式集中维护，成员、作品、沿革、所有权、管理／使用、评价、研究文献不在每类复制同一套列定义。

内容字段保留事实及证据引用，第三部分集中当前争议与支持范围，采集和详细裁决留在 03。时间精度、参与角色、版本及空间范围等影响事实的限定仍保留，不以清理过程为由删除事实；正文关联不自动转正式图谱边。一个实体仍是一份 Markdown，元数据与既有 relations 接口不变，不增加属性 YAML/JSON 副本、额外类型、门禁、独立报告或版本号。

直接依赖同步：ingest/enrich 改为调用对应类型字段及条目规则；共享元数据契约说明分字段属于正文；pipeline 的知识元成稿环节指向该规则；current-requirements 增 R-31，原话与执行范围在 user-revisions REV-041/042。AGENTS 已有入口可正确到达这些文件，无需复制字段规则。实际旧卡在后续成稿／补足整理时原位采用，不建立单独迁移台账。

检查：九类专节与建筑子节齐备，121 行模板表格列数及代码围栏检查通过；18 个相关 Markdown 文件链接有效；skill_registry 检查确认八个 Skill 与派生注册一致，因此无需重导出无变化快照。既有六张已留补足证据的卡片与原指纹一致；git diff --check 通过。仅刷新必要派生指纹清单，未重跑全库测试、外部采集或页面。

本次结果是规则与格式已落地，未批量迁移知识元、增加补足覆盖或改变研究状态；前序未提交研究改动保留。本轮不提交推送，也不清理之前被策略拒绝删除的临时副本。

## REV-049：同步成果并完善卡内链接与状态表达（2026-09-10）

用户要求先提交同步、再更新系统。使用 system-upgrade，按该顺序执行；本轮详细维护过程仅在此记录。

### 同步基线

先核对 main、正式 origin 及全部差异，fetch 后本地 HEAD 与 origin/main 无分叉。有效 298 KU、173 条正式有向关系、10 张有应用记录的卡的最新内容指纹检查通过；原章 Markdown／PDF 指纹不变，Skill 注册及 diff 检查通过。累积 21 个文件提交为 `b5579195978452248f8fc2a425bbe816c28a39c1`，提交说明“完善第一章知识元补足与九类字段化结构”。

执行 push 后，用实际 `git ls-remote --heads origin refs/heads/main` 核对，远端与本地 HEAD 均为上述提交；当时工作树干净。随后才开始本节系统调整。第一章 results 仅校正同步说明，不更改研究状态或重复本节维护报告。

### 问题与修订

- 正文模板原写“指向已有 relations、不复制清单”，容易把避免重复机器数据误解为不显示关系。改为元数据保留唯一机器记录，第三部分提供同一关系的可读含义、方向、知识元链接及对应语境／证据；修改正式记录时同步展示。
- QID 与 Wikipedia 不再只藏在来源或过程记录中：正文骨架给出身份入口的独立字段，显示实际条目、语言与链接。候选不作确认值；身份通过、全文已读、已有补足及全部补足完成分别表述。对齐判据、接口和字段类型不变。
- 内容字段中已有且身份对应的 KU 直接链接固定路径；缺实体或身份未定则保留姓名与说明。词表缺项不阻止有据内容关联展示，导航不自动新增正式边；不为链接扩张实体或关系词表。
- 已有 applied_content_sha256 的卡片后续改写，沿原证据文件追加前后版本记录；不覆盖历史指纹，当前检查取最新已应用记录。普通编辑不因此强制新建机器账本。
- AGENTS 保留原则与正文模板入口；共享字段契约消除“不复制机器记录”与“正文可读展示”的歧义；verify／relate 直接引用该模板。ingest／enrich 现有直接参考已能到达，无须复制规则。pipeline 增加交接时的链接与状态说明，current-requirements 增 R-32。

### 导航与检查

规则漂移检查发现两个问题：模板中的示例相对路径被识别为真实失效链接，已改为文字说明；05 总导航仍称研究未执行、显示 0，而有效登记及既有运行快照均非 0。总导航改指第一章 results、accepted 和分类权威，校正已有三项数字快照，退出过时的分类型空目录入口。旧分类型文件仍保留，图谱 HTML／数据未刷新；索引职责契约明确系统维护可修正总导航，不据此启动呈现。

八个 Skill 的唯一名称、路径和直接参考检查通过，派生注册按新引用刷新；修复后规则漂移为 0。文档修订没有新增 Skill、客户端、门禁、脚本或规则版本；项目仍为 0.1.0。研究卡、来源及正式关系不因这轮系统更新改动；既有卡在实际成稿／补足时逐张采用新展示规则，不宣称全库已迁移。

系统规则、索引与必要指纹清单作为同步之后的本地改动保留，尚未第二次提交或推送。研究补足、正式关系阶段、第六章、涌现及网页没有自动继续。

## REV-056：按实体类型扩展外部数据库（2026-09-11）

用户指出并非所有实体都有QID，外部对齐和补足不能限于Wikipedia、Wikidata。此次将AGENTS、pipeline、verify、enrich和正文身份入口统一改为“类型适用来源”：QID与Wikipedia成为可选身份链；有同粒度准确对象时仍双向核对，缺失时不视为实体失败，也不借相关对象标识。

按现有九类在verify的类型参考中扩展来源。意大利人物可查Treccani《意大利人传记辞典》、SIUSA人物／家族／机构档案生产者记录、SBN及VIAF等规范入口；文献与档案可查保管机构、OPAC SBN、EDIT16、Manus Online、出版社／期刊与Internet Archive等数字化全文；作品和建筑可查ICCD、馆藏及Getty。来源表是按缺口选择的能力矩阵，不是要求逐库遍历的新门禁。

同时明确数据库能力边界：人物辞典正文、规范记录、联合目录、finding aid、馆藏目录和数字化扫描支持的事实范围不同；目录命中不等于原件已读，数字化平台可访问不等于全文已读，聚合记录须保留原始提供者与同源关系。正文模板允许多条权威标识和数字化原件入口，不新增YAML身份副本或中央数据库总账。

本次只调整来源与状态规则并记录REV-056，没有回写第一章实体、改变既有172项Wiki双向配对事实、重做关系、启动知识发现或刷新页面。

## REV-058：修复 Quality gate 的 YAML 日期序列化故障（2026-09-12）

GitHub Actions 运行 `34570941693` 在 Python 3.10 与 3.12 的完整仓库闭环步骤中同时失败；后续提交对应的运行 `34617380471` 复现同一问题。失败发生在 `build_knowledge_graph_data.py`：PyYAML 将未加引号的 ISO 日期解析为 `datetime.date`，生成器把知识元的嵌套元数据直接交给 `json.dumps`，触发 `TypeError: Object of type date is not JSON serializable`。这是 JSON 输出边界的兼容缺陷，不是知识元内容或证据门槛未通过。

生成器现于输出边界递归规范化 YAML 原生值：`date` 与 `datetime` 转为 ISO 字符串，字典、列表和元组递归处理，其他值保持原义。新增回归测试覆盖来源与证据中的嵌套日期，并验证生成的 JavaScript 数据可序列化。修复没有改写知识元事实、关系判断或验证状态，也没有删除或放宽任何检查。

完整闭包继续执行后，暴露出此前生成流程中断所遗留的导航登记不一致：当前类型索引会生成 `archives.md` 与 `families.md`，但输出登记仍指向已退役的 `publications.md`。输出登记已改为当前 archive、family 类型，删除旧 publication 派生索引，并把“研究未执行、成果为空”的过时说明改为第一章样例的真实范围；图谱数据、类型索引、关系／翻译索引、健康快照及生成清单随之刷新。当前图谱派生数据包含 298 个知识元节点和 173 条正式关系；这些是既有有效数据的可重建投影，不代表启动知识发现或页面改版。

首个修复提交 `842efc9` 在本地严格模式通过 18 个步骤，但远端运行 `34620817089` 揭示 Windows 与 Linux 的第二项差异：处理包诊断仍用 `str(Path)` 写入健康快照，Windows 生成反斜杠，Linux 生成正斜杠；载荷变化又使快照时间改取运行机本地时间，连带治理待办日期和生成清单变化。现将这些仓库相对路径统一为 POSIX 表达，使同一提交在不同操作系统得到相同载荷，并让无内容变化时沿用既有快照时间。

本地验证：生成器定向测试 5 项通过；首轮全量测试 258 项通过；`run_sync_closure.py --refresh-generated --full` 的 17 个步骤全部通过，提交后的 `--check-generated` 严格模式也曾通过 18 个步骤。内容审计报告中的开放问题继续作为非阻断研究待办保留，不被本次程序修复冒充为语义验收完成。跨平台路径修复重新生成并测试后，以后续提交对应的新一轮远端 Quality gate 结果作为最终同步验证。

## REV-062–063：内容到知识元、知识元到关系的闭合规则（2026-09-12）

用户以卡拉瓦乔追问人物作品清单、Wikipedia全文阅读、作品实体和创作关系，并进一步要求全盘考虑作品的创作者、委托／赞助、安置／存放，以及人物师承、合作、朋友、亲缘、家庭／家族成员和机构隶属。复核确认旧流程只要求补足内容并审查已有关系端点，没有强制反查“正文中已采纳的具名对象是否已成为 KU”。这使页面虽有全文阅读记录，作品名称仍可能只留在人物卡，关系阶段也只能审查现有端点。

AGENTS、pipeline、ingest正文模板、enrich、relate及关系治理已原位补上闭合规则：具名、可独立识别并承担必需字段或关系端点的对象须建立或复用 KU；外部补足发现后按受影响事实回到知识元，再重走该对象的对齐、补足和关系。范围限于已经采纳的事实，不递归接收网页所有链接。关系审查从正文事实和现有边双向核对，并保留时间、角色、版本、地点作用与证据。

受控词表按当前卡片已经出现的重复角色增加师承、合作、朋友、赞助、父母子女、配偶、手足、雇佣、任命、书信收件、事件参与、取得／所有、机构保管、安置、预定地点、模特和材料供应等关系及反向映射。原有 `associated_*` 不自动批量替换；每条须回读证据后再迁移。该调整没有新增审批、Skill、中央总账或发现层级，也没有把词表存在当作关系已经建立。

必要检查：关系词表为99个唯一类型，新增类型的反向映射可解析；现有219条正式关系无断端点、非法类型、缺反向或弱证据。规则漂移检查为0，241项测试通过，`git diff --check`通过。此次系统修订未提交或推送。

## REV-064：进度审核中的系统一致性检查（2026-09-12）

本轮只审核并记录，不继续系统升级。AGENTS→pipeline→8个Skill及直接参考的入口检查通过，规则漂移0；现行系统没有要求为每阶段增加用户审批。发现以下具体未完成项：

| 问题 | 代码／文件依据及影响 | 后续修复范围 |
|---|---|---|
| 新限定未被关系投影保留 | relate的relation-governance已约定time/role/scope；scripts/build_relation_index.py的_finalize与_build_inverse_relation都不返回这些字段。现有219边也未填写这些字段 | 支持有据限定的原向／反向保留与证据上下文；不对全部边填造时间，不把词表升级当数据迁移完成 |
| 反向展示丢失来源卡语境 | 卡拉瓦乔人物卡把作品卡的“本卡S6”照搬，变成指向另一份sources的编号 | 展示保留原关系来源卡及证据入口；区分派生反向导航和新增事实，不复制第二套断言 |
| 审计仍采用旧契约 | audit_content_quality.py的check_missing_sections仍要求“## 描述”和“## 验证状态”，现行body-template采用“内容／关系与证据”；audit_repo.py仍把未存source_count视为missing_or_invalid，并将298个未分层KU放入research_debt | 按现行最小字段与暂停范围修正检测；source_count/confidence/consensus是按需字段，不能为提高分数补造；未启动的涌现层级不是债务 |
| 有效范围口径混用 | audit_repo统计298 accepted；audit_content_quality及relation consistency扫描317张卡，后者96孤点含19个遗留对象，当前accepted实际77孤点 | 报告明确有效与遗留范围；不直接删除旧资料，不从磁盘数量推导研究进度 |
| 派生记录过时 | relation-index已为219边且与显式边一致；generated-projections-manifest中该输出的SHA256与实际不符；页面数据仍为173边 | 下一次已授权同步前按受影响工件检查生成一致性；页面暂停，不为了机械过关直接刷新全站 |
| 当前结果与历史叙述混杂 | 第一章results约580行（本轮追加前），含历轮成果段；297卡保留REV-060定稿标题，Fontana卡有重复关系区 | 当前结果优先、必要裁决留03；后续整理过时状态和重复段，不删除用户原话、来源或不可替代证据 |

机械结果：rule drift 0；8个Skill注册与直接参考通过；关系检查无断端点、非法类型、缺反向、弱证据；pytest tests目录258项通过。content audit报959项（含旧模板缺区块318、空frontmatter值626等），repo audit结构分61/130；这些输出包含旧规则和遗留对象，不能作为完成率或959个真实业务缺陷。最小元数据与1594条来源所需三个文本子字段经当前契约逐项核查均存在；这只证明结构存在，不证明事实充分。

已核远端main与本地HEAD同为63ecaa465188972766ded24e21643b2625638e19，Quality gate运行34621228757成功。当前未提交修改不在该次CI验证范围；本轮没有执行生成刷新、提交或推送。上述系统问题留作后续有针对性的修复，不新增门禁、Skill或中央审计账本。

## REV-065：关系迁移、投影与审计契约修复（2026-09-12）

REV-064定位的系统缺口已按现行规则修复，没有增加阶段门禁或审批。关系词表补齐本批实际需要的具体动作及反向映射；`apply_relation_plan.py`只接受逐条列明的原关系选择器和完整替换对象，默认dry-run，应用前验证accepted端点、允许类型和全计划可执行性，写入失败时回滚。它不自行推断关系，也不把词表命中当作语义判断。

新增`build_relation_views.py`，以accepted卡片frontmatter为唯一正式关系来源，在卡内生成可读的原向与反向入口，并完整显示时间、角色、范围、原断言卡和证据链接。`build_relation_index.py`同步保留`time`、`role`与`scope`，关系索引不再丢失限定。正文投影可重复执行且保留关系表之外的人工说明；新增测试覆盖限定的原向／反向索引、正文保留和仓库相对证据链接。

三个审计程序改用accepted集合和现行契约。内容质量检查按九类及“内容—描述—关系与证据”结构检查；关系一致性检查不再把未接收遗留卡混入有效关系状态；仓库审计由`sources`推导未显式存储的来源数，并只在涌现结构实际启动后检查层级归属。仓库闭包只在显式传入`--refresh-page`时重建页面数据，当前Quality gate不刷新暂停页面；暂停中的发现、页面和第六章不会被机械审计推动。

本轮实际数据迁移为81项宽泛关系决策生成83条具体边；另因补齐三个必要人物端点形成5条新边，当前accepted为301、正式有向边为226。关系视图共有452行原向／反向导航。

闭包检查还暴露出审计器自身的旧契约误报：它原先会从关系证据抓取`doc_id`并错误要求本地同名目录，把正文已承担的双语字段和可选`verification_level`重新当作frontmatter必填字段，又把旧来源目录、旧处理工件及暂停阶段列入当前P1。现将来源审计限定到`sources`，以显式定位或citation／location判断可追溯性；旧协议和暂停范围只作历史诊断，不参与当前缺陷清单和结构分值。结果为301卡来源可追溯、437条显式定位全部可解析、错误引用0，治理待办的系统P0／P1／P2均为0。

本地完整仓库闭包通过：内容机械缺陷0，关系断端点／非法类型／缺反向映射／弱证据均为0，规则漂移0，8个Skill注册有效，274项测试通过。关系迁移计划在已应用状态下可重复核验，不会将“原宽泛边已不存在”误报为失败。页面数据没有刷新；关系索引、运行状态、导航和生成清单已按其职责更新。远端同步及Quality gate在提交推送后单独核验。
