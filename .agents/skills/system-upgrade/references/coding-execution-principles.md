# 必要实现与验证

先理解任务和现有内容，选择最少够用的实现。直接阅读和语义编辑不能被脚本替代，普通修订不要求额外设计文件或固定测试轮数。
代码只处理有明确机械判据的重复工作、受控写回和展示。改动保持可审阅，保留输入/失败边界；有恢复需求才保存状态。
按实际影响核对结果和直接依赖，运行能验证行为的检查；测试应覆盖真实风险，不只复述实现。通过后无新问题不重复扩展。
不要写入未经证据支持的状态；本地提交与远端一致性分开验证。临时文件无用途时清理，必要证据保留，Git 和当前用户授权边界不由工具名称决定。
