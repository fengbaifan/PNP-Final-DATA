# Git 同步

只使用 main，不创建分支或 worktree。提交/推送须有用户明确授权。
先核对当前差异和正式 origin，fetch 后判断祖先关系，保留用户修改；不强推、不覆盖远端。
按提交变化做必要检查，不把全套测试当每次文档提交的固定前置。
推送后比较本地 HEAD 与远端实际 main，报告是否一致；本地提交不等于远程同步。用户要求“先同步再修改”时先完成同步核验，再进入后续本地工作。
