# 《赞助人与画家》知识系统 — Codex 总入口

## 目标与当前状态

项目版本为初始化阶段的 0.1.0；当前执行第一部分，第一章为样例。当前成果、缺口与完成状态统一见 [第一章结果](04-knowledge/results/patrons-and-painters-chp-1.md)，不由规则版本或文件数量推导完成。第六章后置，知识涌现与页面暂停。有效对象见 [accepted.yml](04-knowledge/accepted.yml)；遗留文件与未处理来源不自动成为成果。

第一部分：知识元与知识图谱，按 **摄入 → 处理 → 知识元 → 对齐 → 补足 → 关系** 执行。
第二部分：知识发现与知识呈现，用户明确启动后，依据 **KU 及关系 → Topic → Theme → Dimension → Domain** 逐级涌现，组织成果并形成页面。

可以预先规定表达类型、证据与交接规则；不得预设具体知识、主题、维度、领域、归属、数量或发现结论。研究材料范围不是涌现后的 Domain。尚未形成的层级是未开展内容，不是待填满的缺陷。

KU 分类按 `01-domain/taxonomy-registry.md` 的语境与对象边界；文献统一为 archive，包含档案和学术文献。城市与政治实体、建筑空间与内部作品须区分。对象数量和行号覆盖不证明提取完整；不能归入当前类型的有意义对象须记录待决，不强塞或漏掉。

## 唯一规则入口

Codex 为本项目执行客户端。规范文件名为 `AGENTS.md`，不另建 agent.md/AGENT.md 同用途入口。本文件规定目标和边界；[pipeline](.agents/pipeline.md) 规定阶段交接；`.agents/skills/<skill-name>/SKILL.md` 规定具体工作。三者属于同一系统，不设重复客户端入口。

用户当前要求优先。事实以来源为依据，历史记录和自动记忆不授权改动。中文沟通，直接阅读、语义分析和撰写为主体；脚本仅辅助必要定位、机械校验、批量写回、索引及页面实现。

## 日常执行

读取输入与上阶段结果 → 完成本阶段语义工作 → 保存过程和当前成果 → 核对直接受影响内容 → 交接。阶段检查不等于逐阶段用户审批；同一判断不重复审核。

- 没有明确研究任务时维护系统，不自动摄入或发现。已有合适输入可继续已授权工作，不为例行阶段转换再次询问。
- 仅交接有充分依据的对象；关键身份、来源或语境缺失时阻断相关对象，其他对象可以继续。审查结束不等于缺口解决。
- 补证、冲突或输入变化时，追踪受影响的知识、关系、发现及页面，回到必要阶段；保留原决定，不全库重跑。
- approved 不等于 applied；无变化记 no_delta，失败不记 completed。机械通过不等于语义验收，自查不称独立验收。
- 知识元的同一性需语义判断。claim 是断言，须由 evidence 支持，不能把断言本身当证明。
- 知识元标题与描述使用中英文；标题为“中文名（English name）”，描述两种语言语义对应。字段及正文规则见 ingest 的直接参考，翻译不新增事实或验证状态。
- 知识元分为统一元数据、按类型组织的内容、关系与证据三部分（REV-019、020）。内容包含双语描述、结构化属性与历史记录；关系与具体事实须对应证据。详细规则见 ingest 字段及正文参考，不维护重复数据副本。
- 正式关系须有明确端点、类型、方向、语境和对应证据。共现、标签、正文链接或 weak_associations 不自动成为关系；派生反向边不新增事实。
- 外部补足依据 Wikipedia、Wikidata、Getty 及适用官方来源；每个实体须按 verify 完成 Wikipedia—Wikidata 双重身份核对，未完成如实记录。身份锚点、链接、来源数量和模型判断不自动证明整篇事实。
- 常规文档和单对象语义编辑直接在授权范围内更新并检查。批量机器写回采用适用接口预检、可审查差异及可恢复提交；已有验证接口遵守 evidence → dry-run → apply 的格式契约，不临时绕开其保护。同一文件串行。
- 只有长任务才保存恢复状态；只检查受影响内容，不默认全套测试、外部采集、刷新全部索引或固定审核轮数。

## 记录与存储

每次收到本项目实际可见用户消息，先在 `06-runtime/governance/user-revisions.md` 追加原话，普通问答也记录；解释和处理结果另列。不虚构其他任务消息。`06-runtime/governance/current-requirements.md` 只索引有效要求。

业务目录从 01 开始，编号表示职责：
- `01-domain/`：材料范围、表达约定与命名，不预设研究结构。
- `02-sources/`：来源资产和登记。来源本体只追加、不改写、不删除；顶层登记可更新。
- `03-processing/`：摄入、处理及知识元/对齐/补足/关系/后续发现的过程记录，另存摄入处理结果与覆盖依据。
- `04-knowledge/`：知识元、断言证据、关系、涌现结构及当前结果说明；过程记录统一在 03-processing。
- `05-outputs/`：呈现过程、定稿与页面；保留现有样式，内容与交互由有效数据驱动。
- `06-runtime/`：用户记录、系统修订和必要运行状态，不替代业务结果。

同一对象固定路径版本更迭；过程保留判断变化，结果指向唯一当前成果，不按日期或“最终版”复制。实际需要时才创建文档，机器工件不重复人工结果。普通规则文件不单独编号版本；项目初始化版本为 0.1.0，Git 和修订日志追溯变化；必要机器协议版本独立保留。

## Skills 与维护

| Skill | 工作 |
|---|---|
| [ingest](.agents/skills/ingest/SKILL.md) | 摄入、处理、知识元成稿，三阶段分别留结果 |
| [verify](.agents/skills/verify/SKILL.md) | 对齐、必要事实验证及真实冲突处理 |
| [enrich](.agents/skills/enrich/SKILL.md) | 针对已有对象的明确缺口补足 |
| [relate](.agents/skills/relate/SKILL.md) | 关系语义审查和正式记录 |
| [synthesize](.agents/skills/synthesize/SKILL.md) | 第二部分逐级涌现及发现，支持重组和反证 |
| [compose](.agents/skills/compose/SKILL.md) | 查询、内容组织、定稿和页面呈现 |
| [inspector](.agents/skills/inspector/SKILL.md) | 按问题检查内容、证据或系统，给出修复去向 |
| [system-upgrade](.agents/skills/system-upgrade/SKILL.md) | 已授权的规则、Skills 与辅助工具调整 |

短规则在 Skill；共享格式或按需工具细节放直接 reference。按实际需要读取，不隐式串联全部技能。

## 渐进式读取与分布记录

- 先读本入口及当前任务结果确定范围和未决项，再读 pipeline 中相关阶段、对应 Skill；遇到字段、引用或机器接口问题时，才读 Skill 直接列出的 reference。参考清单不是全量必读清单，不递归加载所有 Skills、历史日志或来源库；已要求完整语义阅读的材料仍须完整读完。
- 路径以仓库根为准；Skill 自有 `references/` 相对该 Skill，跨 Skill 引用使用完整 `.agents/skills/...` 路径。规则只在职责所属文件维护，导航提供链接，不复制第二份规则或统计。
- 原话仅追加到 user-revisions，current-requirements 仅留有效要求与权威入口；系统修订的详细过程只记 system-upgrade-log。纯系统修订不向 03/04 重复追加业务报告。
- 实际研究过程按 pipeline 分布在 03 的任务包，按阶段追加证据与改变判断的理由；04 的结果原位更新当前状态、成果链接与下一步依赖，历史证据和裁决保留。05 的呈现、06 的治理/机器状态各守职责。
- 接续从当前 results 定位到相关过程段落和证据，不把所有历史记录全文堆进总入口；稳定 task-id、KU 路径、来源定位和 REV 编号连接记录，不增设中央总账或每轮新文档。

只使用 main，不创建其他分支或 worktree。Git 提交、推送按明确授权执行，并单独核验远端。
系统调整过程和结果只记 `06-runtime/governance/system-upgrade-log.md`；权限以实际 Codex 环境为准，不维护另一套权限表或假定 Hook 生效。
